<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dossier Estratégico Moët Hennessy 2026</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800;900&display=swap');
        body { font-family: 'Inter', sans-serif; background-color: #f8fafc; }
        .radar-transition { transition: all 1.5s ease-in-out; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        // Componente de Iconos SVG para evitar errores de librerías externas
        const Icon = ({ name, size = 20, className = "" }) => {
            const icons = {
                globe: <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 18a8 8 0 1 1 8-8 8 8 0 0 1-8 8zM12 4a8 8 0 0 1 0 16 8 8 0 0 1 0-16zM4.93 4.93a10 10 0 0 1 14.14 0M4.93 19.07a10 10 0 0 0 14.14 0"/>,
                layers: <path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.27a1 1 0 0 0 0 1.83l8.57 4.09a2 2 0 0 0 1.66 0l8.57-4.09a1 1 0 0 0 0-1.83zM2 12l10 4.76L22 12M2 16l10 4.76L22 16"/>,
                glass: <path d="M7 2v10a5 5 0 0 0 10 0V2h-2v10a3 3 0 0 1-6 0V2H7zm5 15v5m-3 0h6"/>,
                palette: <circle cx="13.5" cy="6.5" r=".5"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.9 0 1.6-.5 2-1.2.4.7 1.1 1.2 2 1.2a3 3 0 0 0 3-3c0-1.5-1.3-2.7-2.8-2.8.3-.5.5-1.1.5-1.7a6 6 0 0 0-6-6h-1"/>,
                mouse: <path d="m15 2-3.5 7h7L15 2zM8 8l1.5 3L8 14H5l-1.5-3L5 8h3zM19 15c0 3.3-2.7 6-6 6s-6-2.7-6-6 2.7-6 6-6 6 2.7 6 6z"/>,
                share: <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8m-4-6-4-4-4 4m4-4v13"/>,
                zap: <path d="M13 2 3 14h9l-1 8 10-12h-9l1-8z"/>,
                heart: <path d="M19 14c1.5-1.5 3-3 3-5.5a4.5 4.5 0 0 0-9-3c-1.5 1.5-3 3-3 5.5a4.5 4.5 0 0 0 9 3z"/>,
                target: <circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/>
            };
            return (
                <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                    {icons[name] || <circle cx="12" cy="12" r="10"/>}
                </svg>
            );
        };

        const App = () => {
            const [activeTab, setActiveTab] = React.useState('dashboard');
            const [activeBrand, setActiveBrand] = React.useState('moet');
            const [activeMarketBrand, setActiveMarketBrand] = React.useState('moet');

            // --- DATA: INTELLIGENCE HUB REGIONAL ---
            const regionalIntel = [
                { city: "CDMX", intent: "Validación Cultural y Estatus", driver: "Prestigio & Networking VIP", share: 52, color: "#9333ea", searches: "Mesa VIP Zona Maco, Reservados Fine Dining Polanco, Dom Pérignon P2" },
                { city: "Monterrey", intent: "Logro y Legado Familiar", driver: "Éxito & Gifting Premium", share: 18, color: "#d97706", searches: "Moët personalizado, Regalos de lujo San Pedro, Cavas privadas" },
                { city: "Guadalajara", intent: "Exploración de Tendencias", driver: "Innovación & Mixología", share: 12, color: "#2563eb", searches: "Terrazas Spritz, Chandon Garden Gdl, Hotspots 2026" },
                { city: "Tulum", intent: "Hedonismo Solaire", driver: "Estética & Libertad", share: 12, color: "#FFB400", searches: "Beach Clubs Tulum, Rich on Ice Service, Luxury Parties" }
            ];

            // --- DATA: PSICOGRAFÍA DE LUJO ---
            const luxuryPsychography = {
                moet: { name: "Moët & Chandon", persona: "The Success Validator", psych: "Percibe el lujo como un trofeo. Su motivación es la visibilidad del éxito.", luxuryDimensions: { status: 90, heritage: 70, innovation: 40 }, digital: "Instagram Reels, LinkedIn.", behavior: "Ritualización del descorche." },
                veuve: { name: "Veuve Clicquot", persona: "The Creative Aesthetic", psych: "Busca una 'Dolce Vita' creativa y solar. Valora el diseño audaz.", luxuryDimensions: { status: 60, heritage: 55, innovation: 85 }, digital: "Pinterest, TikTok Vlogs.", behavior: "Contenido estético y cromático." },
                dom: { name: "Dom Pérignon", persona: "The Intellectual Collector", psych: "El lujo es conocimiento y misterio. Valora el tiempo y la escasez.", luxuryDimensions: { status: 95, heritage: 90, innovation: 75 }, digital: "Newsletters de inversión.", behavior: "Consumo discreto y ritualizado." },
                chandon: { name: "Chandon", persona: "The Inclusivity Explorer", psych: "Rechaza el lujo rígido. Valora la frescura y la comunidad.", luxuryDimensions: { status: 40, heritage: 30, innovation: 95 }, digital: "TikTok Trends, Spotify.", behavior: "Mixología y consumo casual." }
            };

            // --- DATA: CHANNEL DEEP-DIVE ---
            const searchClusters = {
                onTrade: [
                    { word: "Terrazas Polanco", sentiment: "Positivo", volume: "92/100", seasonality: "Fines de semana" },
                    { word: "Vibe Dining spots", sentiment: "Extremo", volume: "85/100", seasonality: "Jueves - Sábado" },
                    { word: "Beach Clubs Tulum", sentiment: "Positivo", volume: "88/100", seasonality: "Invierno / Vacaciones" }
                ],
                atHome: [
                    { word: "Regalo Moët personalizado", sentiment: "Extremo", volume: "95/100", seasonality: "Mayo / Diciembre" },
                    { word: "Maridaje cena casa", sentiment: "Positivo", volume: "65/100", seasonality: "Planificación" },
                    { word: "E-commerce de lujo", sentiment: "Pragmático", volume: "70/100", seasonality: "Seasonal peaks" }
                ]
            };

            const channelTrends = [
                { month: 'Ene', on: 60, home: 40 }, { month: 'Feb', on: 95, home: 25 }, { month: 'Mar', on: 80, home: 35 },
                { month: 'Abr', on: 75, home: 45 }, { month: 'May', on: 65, home: 85 }, { month: 'Jun', on: 85, home: 40 },
                { month: 'Jul', on: 90, home: 35 }, { month: 'Dic', on: 98, home: 98 }
            ];

            // --- DATA: MARKET SHARE & RADARS ---
            const brandMarketAnalytic = {
                moet: { radar: [90, 60, 85, 75, 60], insight: "Dominancia absoluta en 'Status'.", color: "#C5B358" },
                veuve: { radar: [60, 85, 70, 65, 95], insight: "Liderazgo en 'Design' y Estilo.", color: "#FFB400" },
                dom: { radar: [100, 20, 95, 100, 80], insight: "Máxima 'Exclusividad' Intelectual.", color: "#1A1A1A" },
                chandon: { radar: [30, 100, 45, 35, 90], insight: "Innovación y Consumo Casual.", color: "#E30613" }
            };

            const getRadarPoints = (values) => {
                const radius = 100;
                const center = 150;
                return values.map((v, i) => {
                    const angle = (i * 72 - 90) * Math.PI / 180;
                    const r = (v / 100) * radius;
                    return `${center + r * Math.cos(angle)},${center + r * Math.sin(angle)}`;
                }).join(' ');
            };

            return (
                <div className="min-h-screen bg-[#f1f5f9] flex">
                    {/* SIDEBAR */}
                    <div className="w-24 md:w-80 bg-[#0f172a] text-white flex flex-col h-screen sticky top-0 border-r border-slate-800">
                        <div className="p-10 border-b border-slate-800">
                            <h1 className="text-2xl font-black tracking-tighter leading-none italic uppercase">MH <br/><span className="text-[#C5B358]">PR INTEL</span></h1>
                        </div>
                        <nav className="flex-1 overflow-y-auto pt-8 space-y-2 px-4">
                            {[
                                { id: 'dashboard', label: 'Intelligence Hub', icon: 'globe' },
                                { id: 'profiles', label: 'Psicografía de Lujo', icon: 'layers' },
                                { id: 'occasions', label: 'Occasion DNA', icon: 'glass' },
                                { id: 'channels', label: 'Channel Deep-Dive', icon: 'mouse' },
                                { id: 'share', label: 'Market Share 360', icon: 'share' },
                            ].map((tab) => (
                                <button key={tab.id} onClick={() => setActiveTab(tab.id)} className={`w-full flex items-center p-4 rounded-2xl transition-all duration-300 ${activeTab === tab.id ? 'bg-white text-slate-900 font-bold' : 'hover:bg-slate-800 text-slate-500'}`}>
                                    <Icon name={tab.icon} className="mr-4" />
                                    <span className="hidden md:inline text-[10px] uppercase font-black tracking-widest">{tab.label}</span>
                                </button>
                            ))}
                        </nav>
                    </div>

                    {/* MAIN AREA */}
                    <div className="flex-1 overflow-y-auto">
                        <header className="sticky top-0 bg-white/90 backdrop-blur-xl z-40 px-12 py-8 border-b border-slate-200 flex justify-between items-center shadow-sm">
                            <h2 className="text-2xl font-black tracking-tighter uppercase text-slate-900">PR Strategic Dashboard 2026</h2>
                        </header>

                        <div className="p-12 max-w-[1600px] mx-auto space-y-24">
                            
                            {/* INTELLIGENCE HUB */}
                            {activeTab === 'dashboard' && (
                                <div className="space-y-12">
                                    <h1 className="text-7xl font-black tracking-tighter uppercase">Regional <br/> Hub</h1>
                                    <div className="grid grid-cols-1 lg:grid-cols-2 gap-10">
                                        {regionalIntel.map((region, i) => (
                                            <div key={i} className="bg-white p-12 rounded-[4rem] shadow-xl border-l-[12px]" style={{borderLeftColor: region.color}}>
                                                <h4 className="text-4xl font-black uppercase mb-6">{region.city}</h4>
                                                <div className="grid grid-cols-2 gap-8 mb-8">
                                                    <div className="p-6 bg-slate-50 rounded-3xl">
                                                        <p className="text-[10px] font-black uppercase text-indigo-500 mb-2">Intent</p>
                                                        <p className="text-sm font-bold">{region.intent}</p>
                                                    </div>
                                                    <div className="p-6 bg-slate-50 rounded-3xl">
                                                        <p className="text-[10px] font-black uppercase text-red-500 mb-2">Driver</p>
                                                        <p className="text-sm font-bold">{region.driver}</p>
                                                    </div>
                                                </div>
                                                <p className="text-xs text-slate-400 font-bold italic tracking-tighter uppercase">Búsquedas: "{region.searches}"</p>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            )}

                            {/* PSICOGRAFÍA */}
                            {activeTab === 'profiles' && (
                                <div className="space-y-12">
                                    <h1 className="text-7xl font-black tracking-tighter uppercase italic">Psychography</h1>
                                    <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
                                        {Object.keys(luxuryPsychography).map((key) => (
                                            <div key={key} className="bg-white p-16 rounded-[4.5rem] shadow-2xl group relative overflow-hidden">
                                                <h4 className="text-4xl font-black uppercase mb-2 leading-none">{luxuryPsychography[key].name}</h4>
                                                <p className="text-[#C5B358] font-black text-xs uppercase mb-8">{luxuryPsychography[key].persona}</p>
                                                <div className="p-10 bg-slate-50 rounded-[3rem] italic text-lg mb-10">"{luxuryPsychography[key].psych}"</div>
                                                <div className="grid grid-cols-2 gap-8">
                                                    <div className="space-y-4">
                                                        {Object.entries(luxuryPsychography[key].luxuryDimensions).map(([dim, val]) => (
                                                            <div key={dim}>
                                                                <div className="flex justify-between text-[10px] font-black uppercase mb-1"><span>{dim}</span><span>{val}%</span></div>
                                                                <div className="h-1 bg-slate-200 rounded-full overflow-hidden">
                                                                    <div className="h-full bg-slate-900" style={{width: `${val}%`}}></div>
                                                                </div>
                                                            </div>
                                                        ))}
                                                    </div>
                                                    <div className="text-xs space-y-4 font-bold uppercase">
                                                        <p><span className="text-slate-400">Espacios:</span> <br/>{luxuryPsychography[key].digital}</p>
                                                        <p><span className="text-slate-400">Behavior:</span> <br/>{luxuryPsychography[key].behavior}</p>
                                                    </div>
                                                </div>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            )}

                            {/* CHANNEL DEEP-DIVE */}
                            {activeTab === 'channels' && (
                                <div className="space-y-20">
                                    <h1 className="text-7xl font-black tracking-tighter uppercase">Channel <br/> Trends</h1>
                                    
                                    {/* Timeline Visual */}
                                    <div className="bg-white p-16 rounded-[4.5rem] shadow-2xl">
                                        <h3 className="text-2xl font-black uppercase mb-12">Search Volume (On-Trade vs At-Home)</h3>
                                        <div className="h-[300px] flex items-end gap-2 px-10">
                                            {channelTrends.map((d, i) => (
                                                <div key={i} className="flex-1 flex gap-1 h-full items-end group">
                                                    <div className="flex-1 bg-indigo-600/30 rounded-t-lg group-hover:bg-indigo-600 transition-all" style={{height: `${d.on}%`}}></div>
                                                    <div className="flex-1 bg-emerald-500/30 rounded-t-lg group-hover:bg-emerald-500 transition-all" style={{height: `${d.home}%`}}></div>
                                                </div>
                                            ))}
                                        </div>
                                    </div>

                                    {/* Tablas de Clusters */}
                                    {['onTrade', 'atHome'].map(cType => (
                                        <div key={cType} className="bg-white rounded-[4rem] shadow-2xl overflow-hidden border-t-[15px]" style={{borderColor: cType === 'onTrade' ? '#4f46e5' : '#10b981'}}>
                                            <div className="p-10 bg-slate-50 border-b border-slate-100 flex justify-between items-center">
                                                <h3 className="text-3xl font-black uppercase italic">{cType === 'onTrade' ? 'On-Trade (Hotspots)' : 'At-Home (Intimacy)'}</h3>
                                            </div>
                                            <table className="w-full text-left">
                                                <thead className="bg-slate-100/50">
                                                    <tr>
                                                        <th className="p-8 text-[11px] font-black uppercase text-slate-400">Keyword Cluster</th>
                                                        <th className="p-8 text-[11px] font-black uppercase text-slate-400">Sentiment</th>
                                                        <th className="p-8 text-[11px] font-black uppercase text-slate-400">Volume Index</th>
                                                        <th className="p-8 text-[11px] font-black uppercase text-slate-400">Seasonality</th>
                                                    </tr>
                                                </thead>
                                                <tbody className="divide-y divide-slate-100">
                                                    {searchClusters[cType].map((row, i) => (
                                                        <tr key={i} className="hover:bg-slate-50 transition-colors">
                                                            <td className="p-8 font-black text-slate-800 italic uppercase">"{row.word}"</td>
                                                            <td className="p-8"><span className="px-4 py-1.5 bg-slate-900 text-white rounded-full text-[10px] font-black uppercase">{row.sentiment}</span></td>
                                                            <td className="p-8 text-sm font-black text-slate-400 font-mono">{row.volume}</td>
                                                            <td className="p-8 text-[10px] font-bold text-slate-500 uppercase tracking-widest">{row.seasonality}</td>
                                                        </tr>
                                                    ))}
                                                </tbody>
                                            </table>
                                        </div>
                                    ))}
                                </div>
                            )}

                            {/* MARKET SHARE 360 */}
                            {activeTab === 'share' && (
                                <div className="space-y-16">
                                    <h1 className="text-7xl font-black tracking-tighter uppercase">Market <br/> Per-Brand</h1>
                                    <div className="flex flex-wrap gap-4 mb-10 bg-white p-4 rounded-[3rem] shadow-xl w-fit border border-slate-100">
                                        {['moet', 'veuve', 'dom', 'chandon'].map(key => (
                                            <button key={key} onClick={() => setActiveMarketBrand(key)} className={`px-10 py-4 rounded-full text-[11px] font-black uppercase transition-all ${activeMarketBrand === key ? 'bg-slate-900 text-white' : 'bg-transparent text-slate-400 hover:text-slate-900'}`}>
                                                {key.toUpperCase()} {key === 'dom' ? 'PÉRIGNON' : ''}
                                            </button>
                                        ))}
                                    </div>
                                    <div className="grid grid-cols-1 lg:grid-cols-2 gap-16">
                                        <div className="bg-white p-20 rounded-[5rem] shadow-2xl flex flex-col items-center">
                                            <h3 className="text-3xl font-black mb-16 uppercase italic">Attribute Radar Map</h3>
                                            <svg viewBox="0 0 300 300" className="w-full max-w-[350px]">
                                                {[100, 66, 33].map(r => <circle key={r} cx="150" cy="150" r={r} fill="none" stroke="#ddd" strokeWidth="0.5" strokeDasharray="5 5" />)}
                                                {[0, 72, 144, 216, 288].map(deg => <line key={deg} x1="150" y1="150" x2={150 + 100 * Math.cos((deg-90)*Math.PI/180)} y2={150 + 100 * Math.sin((deg-90)*Math.PI/180)} stroke="#ddd" strokeWidth="0.5" />)}
                                                <polygon points={getRadarPoints(brandMarketAnalytic[activeMarketBrand].radar)} fill={brandMarketAnalytic[activeMarketBrand].color} opacity="0.4" stroke={brandMarketAnalytic[activeMarketBrand].color} strokeWidth="4" className="radar-transition" />
                                                <text x="150" y="25" textAnchor="middle" fill="#999" fontSize="10" fontWeight="black" className="uppercase">Heritage</text>
                                                <text x="270" y="140" textAnchor="start" fill="#999" fontSize="10" fontWeight="black" className="uppercase">Versatility</text>
                                                <text x="230" y="270" textAnchor="start" fill="#999" fontSize="10" fontWeight="black" className="uppercase">Status</text>
                                                <text x="70" y="270" textAnchor="end" fill="#999" fontSize="10" fontWeight="black" className="uppercase">Exclusivity</text>
                                                <text x="30" y="140" textAnchor="end" fill="#999" fontSize="10" fontWeight="black" className="uppercase">Design</text>
                                            </svg>
                                        </div>
                                        <div className="bg-slate-900 text-white p-20 rounded-[5rem] flex flex-col justify-center border-t-[20px]" style={{borderColor: brandMarketAnalytic[activeMarketBrand].color}}>
                                            <h3 className="text-5xl font-black uppercase italic mb-8">Strategic <br/> Insight</h3>
                                            <p className="text-2xl font-light italic opacity-80 leading-relaxed mb-10">"{brandMarketAnalytic[activeMarketBrand].insight}"</p>
                                            <div className="p-8 bg-white/5 rounded-3xl border border-white/10">
                                                <p className="text-[10px] font-black uppercase text-[#C5B358] mb-2 tracking-widest">Market Status 2026</p>
                                                <p className="text-sm font-bold uppercase tracking-tighter">Liderazgo en {activeMarketBrand === 'dom' ? 'Rareza & Valor de Inversión' : activeMarketBrand === 'veuve' ? 'Estética & Lifestyle Solar' : 'Validación Social'}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            )}

                            <div className="text-center text-slate-300 font-black uppercase tracking-[0.5em] py-20 border-t border-slate-100">End of Intel Report</div>
                        </div>
                    </div>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
